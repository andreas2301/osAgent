# Phase 1.6 — Summary

**Completed:** 2026-06-12 (committed at 269b050)
**Status:** ✅ Quick-gate 34/34. STRIP-05 + INSTALL-01 covered.

## What shipped

### Gateway strip (STRIP-05)
- `crates/zeroclaw-gateway/src/` reduced from 22 to 7 source files.
- **Deleted**: `acp.rs`, `api_config.rs`, `api_onboard.rs`, `api_pairing.rs`, `api_personality.rs`, `api_plugins.rs`, `api_webauthn.rs`, `canvas.rs`, `hardware_context.rs`, `openapi.rs`, `openapi_docs.html`, `sse.rs`, `static_files.rs`, `tls.rs`, `voice_duplex.rs`, `ws_approval.rs`.
- **Kept**: `ws.rs` (the `/ws/chat` endpoint, required by OS-MDashboard chat-relay), `auth_rate_limit.rs` (paired_tokens auth path), `api.rs`, `node_tool.rs`, `nodes.rs`, `session_queue.rs`, `lib.rs`.
- `lib.rs` mod declarations stripped to match.

### install-guide ansible task (INSTALL-01)
- Branch `feat/osagent-install-task` cut off main in `sovereign-shield-install-guide`.
- New file: `ansible/install_osagent.yml` (168 lines).
- Structural template mirroring `install_zeroclaw.yml`:
  - Phase ordering preserved.
  - mTLS cert provisioning via Vault PKI single source of truth.
  - Sandbox-allowed home-mirror cert paths.
  - ExecStartPre preflight pattern documented (`osagent-preflight.sh` deferred to M2).
  - AMQP env file at `/etc/zeroclaw/engineer-amqp.env`.
- `meta: end_play` guard at top prevents accidental deploy of M1 hollow placeholders.
- 14 install-guide invariant classes referenced in header comments (class #1 parallel-script divergence, class #2 service restart after envfile, class #10 upstream installer pin-drift, class #15 restart-loop journald flooding).
- PR ready at https://github.com/andreas2301/sovereign-shield-install-guide/pull/new/feat/osagent-install-task

## Requirements coverage

| Requirement | Status | Evidence |
|---|---|---|
| **STRIP-05** Gateway sub-surface stripped; `/ws/chat` + `paired_tokens` kept | ✅ | 16 files deleted; `ws.rs` retained; tests `STRIP-05 *` and `/ws/chat * kept` all green. |
| **INSTALL-01** install-guide companion PR open | ✅ | Branch + file + PR-ready URL; `meta:end_play` guard prevents accidental deploy at M1. |

## Test discipline

Phase 1.6 ratified the TDD pattern established in earlier phases: test-first, implementation-second.

```
=== 01.6 — Gateway Fork + Install Drop-In ===
... 32 STRIP-05 file-absent + mod-decl-absent assertions
✓ /ws/chat endpoint source kept
✓ /ws/chat mod declaration kept
✓ ansible/install_osagent.yml present on feat/osagent-install-task (PR pending merge)
=== 01.6 — Gateway Fork + Install Drop-In: 34 passed, 0 failed ===
```

## Verification

See [01.6-VERIFICATION.md](01.6-VERIFICATION.md).
